{
  "name": "sk-eth-cli",
  "module": "src/main.ts",
  "type": "module",
  "scripts": {
    "build": "bun build --compile src/main.ts --outfile=bin/sk-eth-cli"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^11.1.0",
    "ethers": "^6.8.1"
  }
}
